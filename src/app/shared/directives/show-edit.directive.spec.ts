import { ShowEditDirective } from './show-edit.directive';

describe('ShowEditDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowEditDirective();
    expect(directive).toBeTruthy();
  });
});
