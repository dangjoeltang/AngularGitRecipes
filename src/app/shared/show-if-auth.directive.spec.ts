import { ShowIfAuthDirective } from './show-if-auth.directive';

describe('ShowIfAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfAuthDirective();
    expect(directive).toBeTruthy();
  });
});
